# K-Means Clustering

این پروژه پیاده‌سازی الگوریتم خوشه‌بندی **KMeans** به‌صورت کاربردی و قابل اجرا در **Jupyter Notebook** است.  
بهترین تعداد خوشه بر اساس **Silhouette Score** انتخاب شده و نتیجه خوشه‌بندی به‌صورت گرافیکی نمایش داده می‌شود.

📂 ریپازیتوری پروژه:  
[GitHub Link](https://github.com/ArianKhazaei/small-AI-project/tree/main/K-means_clustring)

---

## 🚀 معرفی

الگوریتم **KMeans** یکی از روش‌های پایه در خوشه‌بندی داده‌هاست.  
تمرکز پروژه:

- تولید داده‌های مصنوعی خوشه‌ای
- اجرای KMeans روی داده‌ها
- انتخاب خودکار بهترین تعداد خوشه با **Silhouette Score**
- نمایش بصری نتایج

---

## ✨ قابلیت‌ها

### 1️⃣ تولید داده‌های پیچیده
```python
generate_complex_clusters(n_clusters, complexity=1.5, size=64)
```

ساختارهای غیرخطی و چندمرکزی

کنترل پیچیدگی داده با پارامتر complexity

### 2️⃣ اجرای KMeans با انتخاب خودکار K
```python
kmeans_clustering(points, max_clusters=20, plot=False)
```
محاسبه Silhouette Score برای مقادیر مختلف K

انتخاب بهترین K و بازگرداندن خوشه‌های نهایی

رسم نتایج قبل و بعد از خوشه‌بندی (در صورت فعال بودن plot=True)

### 3️⃣ نمایش داده و خوشه‌ها
```python
plot_data(data)
plot_clusters(clusters)
```
![مثال](plot.png)
سمت چپ: داده اولیه

سمت راست: داده خوشه‌بندی‌شده

### 4️⃣ ارزیابی کیفیت مدل
```python
model_accuracy(guessed_clusters, clusters)
```

بررسی کیفیت خوشه‌ها بر اساس فاصله نقاط از مراکز

## 🛠️ اجرای پروژه
# کلون ریپازیتوری و باز کردن نوت‌بوک
```
git clone https://github.com/ArianKhazaei/small-AI-project.git
cd small-AI-project/K-means_clustring
jupyter notebook K-means.ipynb
```
## 📌 وابستگی‌ها
```
pip install numpy matplotlib scipy scikit-learn
```

کتابخانه‌های استفاده‌شده: numpy, matplotlib, scipy, scikit-learn

## ⚡ نکات فنی

انتخاب K با بیشینه‌سازی Silhouette Score

داده‌ها شامل ساختارهای غیرخطی هستند و KMeans روی Spiral ممکن است عملکرد ایده‌آل نداشته باشد

پروژه مناسب تحلیل محدودیت‌های KMeans است

## 🔮 توسعه‌های آینده

مقایسه با DBSCAN

اضافه کردن نمودار Silhouette برای هر K

نمایش مراکز خوشه‌ها روی نمودار

تبدیل پروژه به ماژول قابل import

استفاده از داده‌های واقعی برای تست





